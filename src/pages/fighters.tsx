import s from '../styles/fighters.module.scss'

const Fighters = () => {
     return (
          <div>
               <h1 className={s.title}>fighters</h1>
          </div>
     );
};

export default Fighters;
